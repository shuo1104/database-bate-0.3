==================================================
PhotoPolymer 配方数据库 - 部署总结
==================================================

✅ 已修复的问题：
==================================================

1. 后端服务配置
   - 虚拟环境路径：venv → ~/venv/database/
   - 启动命令：去掉多余的 "run" 参数
   - Type: notify → simple

2. 前端Nginx配置
   - 端口：80 → 8080（避免需要root权限）
   - 允许IP访问：server_name localhost _
   - 缓存策略：index.html不缓存，JS/CSS长期缓存

3. Vite构建配置
   - 移除手动chunk配置，避免循环依赖错误

4. 文件权限
   - /home/xgs: 750 → 711
   - 项目目录及子目录：755

5. 端口冲突
   - 提示：8000端口可能被mineru-api占用，需手动停止

==================================================
✅ 配置文件位置：
==================================================

后端服务：backend_fastapi/depoly/photopolymer-api.service
前端Nginx：frontend_vue3/depoly/photopolymer-frontend.conf
Vite配置：frontend_vue3/vite.config.ts

==================================================
✅ 部署命令：
==================================================

方式1：简单部署（需要已安装nginx）
  bash deploy.sh

方式2：自动安装nginx并部署
  bash install_and_deploy.sh

==================================================
✅ 访问地址：
==================================================

前端：http://localhost:8080 或 http://YOUR_IP:8080
后端：http://localhost:8000
文档：http://localhost:8000/docs

==================================================
💡 重要提示：
==================================================

1. 虚拟环境必须在：~/venv/database/
2. 如果8000端口被占用，执行：sudo pkill -f mineru-api
3. 首次访问前端按 Ctrl+Shift+R 清除浏览器缓存
4. 确保目录权限正确（见部署说明.md）

==================================================

